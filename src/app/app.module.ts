// AppModule intentionally left minimal to avoid conflicts with standalone bootstrap.
// The application uses `bootstrapApplication` in main.ts and standalone components.
export const AppModuleUnused = true;